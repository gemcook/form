/* @flow */
type Props = {
  type: string,
  placeholder: string,
  input: Object,
  fluid: boolean,
  meta: Object,
  disabled: boolean,
  maxLength: number,
  outline: boolean,
  dark: boolean,
  autocomplete: string,
};

export type {Props};
